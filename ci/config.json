pipeline {
    agent none
    stages {
        stage('Build Gateway') {
            agent {docker { image 'netflix-api-gateway' }}
            steps {
                dir('gateway') {
                sh 'entrypoint.sh'
                sh '...'
                }
            }
        }
    }
}
